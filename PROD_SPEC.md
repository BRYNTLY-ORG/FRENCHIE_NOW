---
repository: "[Repository Name]"
mission_critical: true
primary_language: "[e.g., TypeScript, Python, Bash]"
health_check_url: "[e.g., https://example.bryntly.org/health]"
sla_target: "99.9%"
deploy_target: "[e.g., AWS Lambda, GitHub Pages, Tailscale Node]"
trusted_domains: ["github.com", "api.github.com", "controlplane.tailscale.com", "log.tailscale.com", "login.tailscale.com"]
---

# Production Specification: [Repository Name]

This document serves as the **Living Contract** for this repository. It defines the product mission, deployment targets, health baselines, and critical requirements. 

> **Role for AI Agents**: Refer to the YAML metadata above and the sections below as the primary source of truth for repository purpose and production safety during triage, feature development, or refactoring.

## 1. Product Mission
[Describe in 1-2 sentences what this repository ships and who the primary users are.]

## 2. Deployment & Environments
- **Production URL**: [e.g., https://example.bryntly.org]
- **Target Infrastructure**: [e.g., GitHub Pages, AWS Lambda, Tailscale-protected node]
- **Deploy Trigger**: [e.g., Push to `main`, Manual workflow dispatch]
- **Rollback Protocol**: [e.g., Revert `main` and push, or run `gh workflow run rollback.yml`]

## 3. Critical Health Baselines
A "Healthy" state for this repository is defined by:
- **Core Endpoint**: `GET /` returns `200 OK` in < 500ms.
- **Uptime Target**: 99.9%
- **Critical Path**: [e.g., Form submission must succeed and trigger a downstream webhook.]

## 4. Core Dependencies
This repository requires the following to function:
- **Internal**: [e.g., Internal: Shared_DB]
- **External APIs**: [e.g., External: GitHub_API, External: Ollama]
- **Databases**: [e.g., External: S3_Bucket]

## 5. Security & Secrets
The following secrets/variables must be configured for operational success:
- `AGENT_APP_ID` (Org-level)
- `AGENT_APP_PRIVATE_KEY` (Org-level)
- [Add repo-specific secrets here]

## 6. Agentic Guidelines
- **Triage Priority**: [e.g., Prioritize failures in the Quote flow over style inconsistencies.]
- **Safety**: [e.g., Never modify the `PROD_SPEC.md` file without explicit human approval.]
- **Verification**: [e.g., Every PR must pass `lint-and-test` before being considered for auto-merge.]
- **Spec Sign-off**: Before claiming a feature complete, agents must verify the implementation against the **Critical Path** defined in Section 3.

---
*Generated by REPO_BOOTSTRAP. Do not remove this document; update it as the repository's mission evolves.*
